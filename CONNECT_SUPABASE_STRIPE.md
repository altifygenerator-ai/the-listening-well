# Connection guide moved

Use `WEBHOOK_ADMIN_SETUP.md`. It contains the current Supabase migration, separate copper/moon wallets, Stripe webhook events, catalog verification, and the admin setup for `altifygenerator@gmail.com`.
